services:
  db:
    image: postgres:16-alpine
    container_name: pusmanpro-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-pusmanpro}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pusmanpro}
      POSTGRES_DB: ${POSTGRES_DB:-pusmanpro_db}
    ports:
      - "5433:5432"
    volumes:
      - pusmanpro_pgdata:/var/lib/postgresql/data

volumes:
  pusmanpro_pgdata:
